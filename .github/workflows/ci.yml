name: Core CI/CD

on:
  push:
    branches: [DeployTest]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        run: |
          TAG=${GITHUB_SHA}
          # استخدم github.repository بدل github.repository_owner
          IMAGE=ghcr.io/${{ github.repository }}/core-service:$TAG
          docker build -t $IMAGE .
          docker push $IMAGE